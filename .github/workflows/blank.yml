name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Run a multi-line script
      env:
        CTX: ${{toJson(github)}}
      run: |
        echo You just pushed ${CTX}
